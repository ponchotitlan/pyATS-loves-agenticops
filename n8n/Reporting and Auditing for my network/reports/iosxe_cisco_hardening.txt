- File Prefix: hardening_status
- Query: Assess IOS-XE device R1 for compliance against the current Cisco IOS-XE hardening guide. One row per rule in Analysis & Findings: rule, severity, config evidence, verdict.
- Notes: Fetch https://sec.cloudapps.cisco.com/security/center/resources/IOS_XE_hardening once and derive the rule set from what the page actually says. The guide is revised periodically, so never substitute a remembered or assumed rule list. Then call pyats_show_running_config on R1 once and evaluate every derived rule against that single output. Two tool calls total, no per-rule commands. For each rule record the config line or command that evidences it and a verdict of PASS, FAIL, or NOT ASSESSED when the running config cannot show it. Assign each rule CRITICAL, HIGH or MEDIUM from the guide's own emphasis and the exposure a failure creates. Carry severity into Risks & Considerations and give each FAIL one remediation command in Recommendations. State in Scope & Assumptions the date you retrieved the guide and how many rules you derived. If the fetch fails or the content is truncated, say so there, list the sections you could read, and assess only those rather than filling gaps from memory.
